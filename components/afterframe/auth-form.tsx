'use client';
import {useState} from 'react';
import {Tabs,TabsList,TabsTrigger} from '@/components/ui/tabs';
export function AuthForm({onAuthenticated}:{onAuthenticated:()=>Promise<void>}){
 const [mode,setMode]=useState('login'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[username,setUsername]=useState(''),[busy,setBusy]=useState(false),[message,setMessage]=useState('');
 return <div className="stack"><Tabs value={mode} onValueChange={v=>{setMode(String(v));setMessage('')}}><TabsList><TabsTrigger value="login">Sign in</TabsTrigger><TabsTrigger value="signup">Create account</TabsTrigger></TabsList></Tabs><form className="stack" onSubmit={async e=>{e.preventDefault();setBusy(true);setMessage('');try{const r=await fetch('/api/auth',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:mode,email,password,username})});const d=await r.json() as any;if(!r.ok)throw new Error(d.error);setPassword('');if(d.authenticated)await onAuthenticated();else {setMessage(d.message);setMode('login');}}catch(e){setMessage((e as Error).message)}finally{setBusy(false)}}}>{mode==='signup'&&<label>Username<input required maxLength={60} autoComplete="nickname" value={username} onChange={e=>setUsername(e.target.value)}/></label>}<label>Email<input required type="email" maxLength={254} autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input required type="password" minLength={8} maxLength={128} autoComplete={mode==='login'?'current-password':'new-password'} value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="lime" disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign in':'Create account'}</button></form>{message&&<p role="status">{message}</p>}<p className="optional-hint">Sign in to save films and connect with your club. New accounts may need email confirmation.</p></div>
}
